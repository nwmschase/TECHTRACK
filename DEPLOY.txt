TechTrack v4.8.0 — 10-hour signed login cookie

1. Add AUTH_COOKIE_SECRET in Streamlit Cloud → App settings → Secrets.
   AUTH_COOKIE_SECRET = "paste-a-long-random-string-here"
   python -c "import secrets; print(secrets.token_hex(32))"
2. Wait for Streamlit to rebuild after rv_techtrack.py lands on main, then Reboot the app.
3. Sidebar should read: v4.8.0 • 10-hour login cookie
4. Sign in once. Close the tab / lock the phone. Reopen the app — you should still be signed in.
5. Log out must return to the login screen.
